services:
  - type: web
    name: gerador-imagem-ia
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HF_TOKEN
        sync: false
      - key: HF_MODEL
        value: black-forest-labs/FLUX.1-schnell
